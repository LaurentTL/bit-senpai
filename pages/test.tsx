import type { NextPage } from 'next';
import Charts from '../components/Charts';

const Test: NextPage = () => {
    return (
        <div>
            <Charts />
        </div>
    );
};

export default Test;
